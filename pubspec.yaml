name: miswag_auth
description: A drop-in authentication and session flow management plugin.
version: 0.1.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: "<=6.0.3"
  flutter_bloc: ^7.2.0
  equatable: ^1.2.2
  shared_preferences: ^0.5.8
  dio: ^3.0.9
  get_it: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
